The goal of the notebook was to train a PyTorch model to classify natural text as postive, neutral and negative using the reviews from multiple apps as the train data.


Links to the data can be found in the notebook.
